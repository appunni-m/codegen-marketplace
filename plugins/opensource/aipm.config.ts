import { defineConfig } from '@ai-plugin-marketplace/core';

export default defineConfig({
  version: '0.1.0',
  targets: ['claude', 'codex', 'cursor', 'vercel'],
  description:
    'Evidence-first open-source guidance for documentation, Makefile interfaces, and live-oracle migration parity tests.',
  keywords: ['documentation', 'makefile', 'migration', 'testing', 'open-source'],
});
